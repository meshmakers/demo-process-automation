$schema: https://schemas.meshmakers.cloud/construction-kit-elements.schema.json
types:
  - typeId: MatchStatus
    derivedFromCkTypeId: System/Entity
    associations:
      - id: ${thisModel}/AccountingMatch
        targetCkTypeId: ${thisModel}/Transaction
    attributes:
      - id: ${thisModel}/TransactionStatus
        name: Status
